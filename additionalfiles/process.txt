1. create a python script to generate(or replace) toolkit.properties, connection.json, oracon.txt and pgcon.txt
2. create a python script to combine applications (runMtk, auditTrigger, comPareTool)
3. create a python script to run path in postgres database 